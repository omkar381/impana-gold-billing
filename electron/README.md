# Impana Gold Desktop (Electron)

This folder contains a minimal Electron wrapper that opens the Flask app in a desktop window.

## Run
```bash
npm install
npm run start
```

The wrapper tries to start Flask using the local venv at ../venv.
Ensure you have already run migrations and seed on the same machine.
